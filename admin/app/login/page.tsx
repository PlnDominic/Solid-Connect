'use client';

import { FormEvent, useMemo, useState } from 'react';
import { createClient } from '../../lib/supabase-browser';
import './polish.css';

function EyeIcon({ off = false }: { off?: boolean }) { return <svg aria-hidden="true" viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M2.5 12s3.3-5 9.5-5 9.5 5 9.5 5-3.3 5-9.5 5-9.5-5-9.5-5Z" />{!off && <circle cx="12" cy="12" r="2.2" />}{off && <path d="m4 4 16 16" />}</svg>; }

export default function LoginPage() {
  const [email, setEmail] = useState(''); const [password, setPassword] = useState('');
  const [error, setError] = useState(''); const [pending, setPending] = useState(false); const [showPassword, setShowPassword] = useState(false);
  const checks = useMemo(() => ({ length: password.length >= 8, upper: /[A-Z]/.test(password), number: /\d/.test(password), special: /[^A-Za-z0-9]/.test(password) }), [password]);
  const passwordReady = Object.values(checks).every(Boolean);
  const strengthScore = Object.values(checks).filter(Boolean).length;
  const strengthLabel = ['', 'Weak', 'Fair', 'Good', 'Strong'][strengthScore];
  const strengthLevel = ['', 'weak', 'fair', 'good', 'strong'][strengthScore];
  async function signIn(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setError(''); if (!passwordReady) { setError('Use the checks below to create a stronger password.'); return; } setPending(true); const { error: authError } = await createClient().auth.signInWithPassword({ email, password }); if (authError) { setError(authError.message); setPending(false); return; } window.location.assign('/verifications'); }
  return <main className="login reference-login"><header className="reference-brand"><img src="/logo.jpeg" alt="Solid Connect logo" className="brand-logo" width={30} height={30} /><strong>Solid Connect</strong></header><section className="reference-card" aria-labelledby="login-title"><img src="/logo.jpeg" alt="Solid Connect" className="reference-icon" width={80} height={80} /><h1 id="login-title">Sign in with email</h1><p className="reference-subtitle">Access your operations workspace<br />and keep every decision moving.</p><form onSubmit={signIn}><label className="reference-input"><span aria-hidden="true">✉</span><input name="email" value={email} onChange={event => setEmail(event.target.value)} type="email" autoComplete="email" placeholder="Email" aria-label="Email" required /></label><label className="reference-input"><span aria-hidden="true">▣</span><input name="password" value={password} onChange={event => setPassword(event.target.value)} type={showPassword ? 'text' : 'password'} autoComplete="current-password" placeholder="Password" aria-label="Password" required /><button type="button" onClick={() => setShowPassword(!showPassword)} aria-label={showPassword ? 'Hide password' : 'Show password'}><EyeIcon off={!showPassword} /></button></label><div className={`strength-meter ${password ? 'visible' : ''}`} aria-live="polite"><div className="strength-track"><div className={`strength-fill ${strengthLevel}`} style={{ width: `${(strengthScore / 4) * 100}%` }} /></div>{strengthScore > 0 && <span className={`strength-label ${strengthLevel}`}>{strengthLabel}</span>}<ul className="strength-reqs"><li className={checks.length ? 'pass' : ''}><svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round"><circle cx="8" cy="8" r="6.5" opacity=".5" />{checks.length && <path d="M4.5 8.2l2.2 2.4 4.8-5.2" />}</svg>At least 8 characters</li><li className={checks.upper ? 'pass' : ''}><svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round"><circle cx="8" cy="8" r="6.5" opacity=".5" />{checks.upper && <path d="M4.5 8.2l2.2 2.4 4.8-5.2" />}</svg>One uppercase letter</li><li className={checks.number ? 'pass' : ''}><svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round"><circle cx="8" cy="8" r="6.5" opacity=".5" />{checks.number && <path d="M4.5 8.2l2.2 2.4 4.8-5.2" />}</svg>One number</li><li className={checks.special ? 'pass' : ''}><svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round"><circle cx="8" cy="8" r="6.5" opacity=".5" />{checks.special && <path d="M4.5 8.2l2.2 2.4 4.8-5.2" />}</svg>One special character</li></ul></div><a className="forgot-link" href="mailto:support@solidconnect.co?subject=Admin%20password%20reset">Forgot password?</a><button className="reference-submit" disabled={pending}>{pending ? 'Checking access...' : 'Get Started'}</button>{error && <p className="notice" role="alert">{error}</p>}</form><p className="login-foot">{password ? (passwordReady ? 'Password ready for verification.' : 'Complete the checks to continue.') : 'Access is limited to provisioned administrators.'}</p></section></main>;
}
